interface:
  display_name: "Review Pass"
  short_description: "Read-only review panel pass"
  default_prompt: "Use $review-pass to run one read-only fresh or verification review panel pass on this target with eligible clean-context reviewers; return a structured Markdown review packet, or a blocked result with the prompt packet when clean-context reviewers are unavailable."
