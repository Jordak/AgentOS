#!/usr/bin/env bash
set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"

"$ROOT/scripts/run-validator"

if git -C "$ROOT" diff --cached --quiet --; then
  echo "No staged changes; skipping staged publication scan."
  exit 0
fi

"$ROOT/scripts/check_staged_publication_secrets.sh"
