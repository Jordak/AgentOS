repos:
  - repo: local
    hooks:
      - id: agentos-validation-push-safety
        name: AgentOS validation and staged publication scan
        entry: bash scripts/hooks/pre-commit
        language: system
        pass_filenames: false
